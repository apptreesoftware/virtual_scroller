name: virtual_scroller
description: An absolute bare-bones web app.

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  meta: any

dev_dependencies:
  build_runner: ^0.10.0
  build_web_compilers: ^0.4.0
  http: any
